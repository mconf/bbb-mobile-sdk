import styled from 'styled-components/native';
import Colors from '../../constants/colors';

const TextInput = styled.TextInput`
  flex: 1;
  border: 2px solid ${Colors.lightGray200};
  padding: 16px;
  font-size: 16px;
`;

export default {
  TextInput,
};
