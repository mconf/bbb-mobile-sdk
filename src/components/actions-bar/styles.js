import styled from 'styled-components/native';

const ContainerView = styled.View`
  display: flex;
  flex-direction: row;
  justify-content: center;
  padding-bottom: 10px;
`;

export default {
  ContainerView,
};
